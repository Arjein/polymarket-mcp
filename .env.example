# Polymarket MCP Server – Environment Variables
# Copy this file to .env and fill in your values.

# ─── Authentication (required for trading tools) ─────────────────────
# Your Polygon wallet private key (with 0x prefix).
# Only needed if you want to use the trading tools (place_order, etc.).
# POLYMARKET_PRIVATE_KEY=0x...

# Optional: pre-configured API credentials.
# If omitted, they are derived automatically from the private key.
# POLYMARKET_API_KEY=
# POLYMARKET_API_SECRET=
# POLYMARKET_API_PASSPHRASE=

# Your public wallet address (for positions/activity queries).
# POLYMARKET_WALLET_ADDRESS=0x...

# ─── Safety Guards ───────────────────────────────────────────────────
# Maximum order value in USD (price × size). Orders exceeding this are rejected.
POLYMARKET_MAX_ORDER_SIZE=100

# Set to "true" to simulate orders without executing. Recommended for testing.
POLYMARKET_DRY_RUN=true
